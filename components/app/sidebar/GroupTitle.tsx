import React from 'react'

const GroupTitle = ({title} : {title: string}) => {
  return (
    <div className='text-base text-blossom-400 font-inter uppercase font-semibold'>{title}</div>
  )
}

export default GroupTitle
